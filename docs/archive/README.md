# Documentation archive

Everything in this directory is **historical only and not operational**. It may
describe completed plans, old interfaces, stale deployment state, or instructions
for a different repository. Use current source and the root documentation set
for operations.

| File | Reason archived |
|---|---|
| `GCD-ARCADE-REDESIGN-HANDOFF.md` | implementation brief superseded by the active light-launcher source |
| `CROSS_REPO_INTEGRATION_HANDOFF.md` | point-in-time cross-repository status and deployment claims not verified in this audit |

Retain useful historical context, but do not copy commands or status claims from
these files without re-verifying the current target repository and external
state.

